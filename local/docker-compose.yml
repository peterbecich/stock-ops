version: '3'
services:
  stock-servant:
    build: .
    image: peterbecich/stock-servant-stock-servant-local
    # command: stock-servant
    # expose:
    #     - "8080"
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
  # stock-postgres:
  #   image: postgres:10
  #   restart: always
  #   environment:
  #     - POSTGRES_PASSWORD:'${POSTGRES_PASSWORD}'
  #   expose:
  #     - "5432"

  stock-postgres:
    build:
      context: .
      dockerfile: dockerfiles/postgres.dockerfile
      
    restart: always
    environment:
      - POSTGRES_PASSWORD:'${POSTGRES_PASSWORD}'
    expose:
      - "5432"

  
  stock-redis:
    image: redis:4-alpine
    expose:
      - "6379"
      
    # nginx:
    #     build: ./nginx
    #     image: nginx
    #     ports:
    #         - "8080:80"
    #     depends_on:
    #         - stock-servant
